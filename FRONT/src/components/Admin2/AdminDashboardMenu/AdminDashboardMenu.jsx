import React from 'react';

const AdminDashboardMenu = ({}) => {

    return (
        <div>
            <div>TITLE</div>
            <div>Interaction container</div>
        </div>
    )
}

export default React.memo(AdminDashboardMenu);
